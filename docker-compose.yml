services:
  postgres:
    image: postgres
    container_name: api-werk-postgres
    environment:
      POSTGRES_PASSWORD: 123456
      POSTGRES_USER: postgres
      POSTGRES_DB: dbWerk
    ports:
      - "5433:5432"
    volumes:
      - ./.postgres-data:/var/lib/postgresql
